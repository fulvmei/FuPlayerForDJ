package com.chengfu.android.fuplayer.achieve.dj.demo.video.widget;

import android.content.Context;
import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import android.util.AttributeSet;
import android.widget.FrameLayout;

public class DodoMoveView extends FrameLayout {

    public DodoMoveView(@NonNull Context context) {
        this(context, null);
    }

    public DodoMoveView(@NonNull Context context, @Nullable AttributeSet attrs) {
        super(context, attrs, 0);
    }

    public DodoMoveView(@NonNull Context context, @Nullable AttributeSet attrs, int defStyleAttr) {
        super(context, attrs, defStyleAttr);
    }
}
